# HRMS Frontend (React + Tailwind)

## Setup
1. Install dependencies:
   - `npm.cmd install`
2. Start dev server:
   - `npm.cmd run dev`

Frontend runs on `http://localhost:5173`.

## Notes
The app expects the backend at `http://localhost:3000` with session cookies enabled.